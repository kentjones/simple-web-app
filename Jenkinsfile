pipeline {
	agent any
	/* options {
		skipDefaultCheckout()
	}
	*/
	
	stages {
		stage('Build') {
		
			agent any
			steps {
				echo 'Hello from my first application to run under my Pi web server'
			}
		}
	}
}
