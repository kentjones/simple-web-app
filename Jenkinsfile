pipeline {
	agent none
	
	stages {
		stage('Build') {
		
			agent any
			/* options {
				skipDefaultCheckout()
			}
			*/
			steps {
				echo 'Hello from my first application to run under my Pi web server'
			}
		}
	}
}
