{
  "name": "simple-web-app",
  "version": "1.0.0",
  "description": "Simple web application to count number of hits",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npm run build",
    "dev": "nodemon server.js"
  },
  "author": "Kent B. Jones",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
